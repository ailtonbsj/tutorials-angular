## History of commands

```bash
# Create new Angular project
# Options.: stylesheet: SCSS
ng new dynamic-autocomplete --no-standalone --routing --ssr=false

# Options: Y, Azure/Blue, Y, Include and enable
ng add @angular/material

# Generate School service
ng g s school
```
